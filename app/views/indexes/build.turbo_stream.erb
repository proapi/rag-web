<%# Update flash messages immediately %>
<%= turbo_stream.update "flash-messages" do %>
  <% if flash[:alert] %>
    <div class="mb-6 bg-red-50 border border-red-200 rounded-lg p-4">
      <div class="flex">
        <svg class="h-5 w-5 text-red-400 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <p class="text-sm text-red-800"><%= flash[:alert] %></p>
      </div>
    </div>
  <% end %>
  <% if flash[:notice] %>
    <div class="mb-6 bg-green-50 border border-green-200 rounded-lg p-4">
      <div class="flex">
        <svg class="h-5 w-5 text-green-400 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <p class="text-sm text-green-800"><%= flash[:notice] %></p>
      </div>
    </div>
  <% end %>
<% end %>

<%# Wrap indexes with auto-refresh controller to trigger delayed refresh %>
<%= turbo_stream.update "indexes-wrapper" do %>
  <div data-controller="auto-refresh" data-auto-refresh-delay-value="3000">
    <%= turbo_frame_tag "indexes-list" do %>
      <%= render "list" %>
    <% end %>
  </div>
<% end %>
